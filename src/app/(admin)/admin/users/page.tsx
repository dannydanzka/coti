/**
 * User Manager Page (Admin)
 *
 * Manage users: view, filter by role, create admin, soft delete.
 */

'use client';

import { UserManagerScreen } from '@apps/admin/presentation/screens/UserManagerScreen';

const AdminUsersPage = () => <UserManagerScreen />;

export default AdminUsersPage;
