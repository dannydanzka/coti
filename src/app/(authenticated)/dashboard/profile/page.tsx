/**
 * User Profile Page
 *
 * Profile management page for authenticated users.
 * Allows viewing and editing profile information.
 *
 * @route /dashboard/profile
 */

'use client';

import { ProfileScreen } from '@apps/public/presentation/screens/ProfileScreen';

const ProfilePage = () => <ProfileScreen />;

export default ProfilePage;
