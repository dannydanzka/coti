/**
 * Auth Context - Domain Layer
 *
 * Pure domain layer for authentication following Clean Architecture.
 * Contains only business entities, use cases, and repository interfaces.
 *
 * NO implementations, NO infrastructure, NO UI components.
 * For implementations, see @infrastructure, @presentation, and @apps/public layers.
 */

export * from './domain';
