/**
 * UserFormModal
 *
 * Styled components for UserFormModal.
 */

'use client';

import styled from 'styled-components';

import { spacing } from '@constants';

export const FormActions = styled.div`
  display: flex;
  gap: ${spacing.sm};
  justify-content: flex-end;
  margin-top: ${spacing.md};
`;
