/**
 * Manager Type Definitions
 *
 * Centralized manager interfaces for admin context.
 * Follows Context7 type centralization pattern (Rule 221-230).
 *
 */

export interface Manager {
  id: string;
  name: string;
}
