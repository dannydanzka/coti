/**
 * Assets - Coti platform
 *
 * Image assets should be imported directly from their paths:
 * import Logo from '@assets/branding/Logo.svg';
 * import AuthBackground from '@assets/auth/Background.webp';
 *
 * Direct imports are preferred over barrel exports for images because:
 * 1. Images have default exports (cannot be re-exported without aliases)
 * 2. Tree-shaking works better with direct imports
 * 3. Avoids the no-alias-exports ESLint rule
 */
