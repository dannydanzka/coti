/**
 * HTTP Constants
 *
 * Constants for HTTP operations including error messages and timeouts.
 */

export const HTTP_CONSTANTS = {
  DEFAULT_ERROR_MESSAGE: 'Ha ocurrido un error inesperado. Por favor intenta de nuevo.',
  DEFAULT_TIMEOUT: 30000,
} as const;
